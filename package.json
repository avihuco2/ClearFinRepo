{
  "name": "clearfin-secure-foundation",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "tsc --build",
    "test": "vitest --run",
    "test:watch": "vitest",
    "lint": "tsc --build --dry"
  },
  "devDependencies": {
    "@types/node": "^25.5.2",
    "fast-check": "^3.22.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
